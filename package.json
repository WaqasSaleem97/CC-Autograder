{
  "name": "cc-autograder",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check scripts/create-config.js && node --check scripts/load-students.js && node --check scripts/collect-submissions.js && node --check scripts/grade-submissions.js && node --check scripts/show-results.js && node --check scripts/update-firestore.js"
  },
  "dependencies": {
    "firebase-admin": "^13.4.0"
  },
  "engines": {
    "node": ">=22"
  }
}

