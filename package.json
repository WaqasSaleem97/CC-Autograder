{
  "name": "cc-autograder",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Trusted GitHub Actions autograder for the multi-course Firebase Student Marks Portal.",
  "scripts": {
    "collect": "node scripts/collect-submissions.js",
    "grade": "node scripts/grade-submissions.js",
    "update-firestore": "node scripts/update-firestore.js",
    "check": "node --check scripts/collect-submissions.js && node --check scripts/grade-submissions.js && node --check scripts/update-firestore.js"
  },
  "dependencies": {
    "firebase-admin": "^13.10.0"
  },
  "engines": {
    "node": ">=22"
  }
}
